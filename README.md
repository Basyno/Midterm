# Midterm

## How Execution
```
for TLM:
$ cd midterm 
$ cd bubblesort_tlm
$ cd build
$ cmake ..
$ make run

for Stratus:
$ login the EE workstation
$ cd midterm 
$ cd BASIC or $ cd UNROLL or  $ cd PIPELINE
$ make sim_BASIC_V, make sim_UNROLL_DPA_V, make sim_PIPELINE_DPA_V
```
